|  NAME | PROMT  |DECRIPTION   | EXAMPLE  |
|---|---|---|---|
|  app.yaml | **Requirements:** Use apps/v1 Deployment, set the metadata name to 'main-app', and use a label selector for 'app: web-service'.<br> **Replication:** Set it to 3 replicas for high availability. <br>**Container:** Include a single container using a stable image, and expose port 80 <br> **Constraint:** Keep the manifest minimal and clean so that I can later overlay resource limits, probes, and volume mounts using Kustomize or manual merging. | Create app.yaml manifest    | https://github.com/Vlad1slav1k/AsciiArtify2/blob/main/yaml/app.yaml  |
| app-livenessProbe.yaml  | **Prompt:** "Generate a Kubernetes livenessProbe configuration for a container.<br>**Mechanism:** Use an [HTTP GET / TCP Socket / Exec Command] check.<br>Timing: Set a 30-second initialDelaySeconds to allow for a slow application startup.<br> **Thresholds:** Set it to check every 10 seconds, and only trigger a restart if it fails 5 times in a row. <br>**Thresholds:** Set it to check every 10 seconds, and only trigger a restart if it fails 5 times in a row.   |  Generate a Kubernetes livenessProbe configuration for a container | https://github.com/Vlad1slav1k/AsciiArtify2/blob/main/yaml/app-livenessProbe.yaml  |
| app-readinessProbe.yaml  | To generate a specialized CronJob, you can use a prompt like this:<br>"Generate a Kubernetes CronJob YAML named 'data-sync'. It should run every day at 2:00 AM. Use the 'python:3.9-slim' image to run the command 'python sync_script.py'. Set a concurrencyPolicy to 'Forbid' so jobs don't overlap, and ensure it keeps a history of only the last 3 successful runs."| Create and use  app-readinessProbe.yaml  | https://github.com/Vlad1slav1k/AsciiArtify2/blob/main/yaml/app-readinessProbe.yaml  |
| app-volumeMounts.yaml | If you need to generate specific storage configurations in the future, you can use a prompt structured like this:"Create a Kubernetes Pod YAML that includes volumeMounts. The pod should have one container named 'web-server' using the Nginx image. Mount a volume named 'config-vol' to the path '/etc/nginx/conf.d' as read-only, and a second volume named 'data-vol' using a PersistentVolumeClaim to '/var/www/html'."  |  Create and use  app-volumeMounts.yaml | https://github.com/Vlad1slav1k/AsciiArtify2/blob/main/yaml/app-volumeMounts.yaml  |
| app-cronjob.yaml   | To generate a specialized CronJob, you can use a prompt like this:<br>"Generate a Kubernetes CronJob YAML named 'data-sync'. It should run every day at 2:00 AM. Use the 'python:3.9-slim' image to run the command 'python sync_script.py'. Set a concurrencyPolicy to 'Forbid' so jobs don't overlap, and ensure it keeps a history of only the last 3 successful runs."  |  Create and use  app-cronjob.yaml  | https://github.com/Vlad1slav1k/AsciiArtify2/blob/main/yaml/app-cronjob.yaml  |
| app-job.yaml  | To generate a similar one-time task, you can use a prompt like this:"Generate a Kubernetes Job YAML named 'image-processor'. It should use a custom image 'my-registry/image-tool:v1' and run the command './process-images.sh'. Set a backoffLimit of 2 and ensure the Job is automatically deleted 60 seconds after it completes using ttlSecondsAfterFinished."  |  Create and use  app-job.yaml  | https://github.com/Vlad1slav1k/AsciiArtify2/blob/main/yaml/app-job.yaml  |
|   |   |   |   |
|   |   |   |   |
|   |   |   |   |
